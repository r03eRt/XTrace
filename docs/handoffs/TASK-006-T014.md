# Handoff TASK-006-T014

## Resumen

Se conectó el segundo pase con `POST /search`. El `search_id` se crea antes de
ejecutar el refinamiento; el resultado conserva el ranking base cuando la
fuente no está disponible, y la respuesta expone `refinement` y
`timestamp_provenance` sin alterar los campos existentes. La composición usa
el registry/allowlist del crawler y metadata server-side de `videos`/`sources`;
los vídeos locales no provocan peticiones de red.

## Archivos modificados

- `services/api/xtrace_api/refinement/service.py`
- `services/api/xtrace_api/search_service.py`
- `services/api/xtrace_api/routers/search.py`
- `services/api/tests/integration/test_search.py`
- `services/api/tests/integration/test_parity_cli_api.py`
- `services/api/tests/unit/test_search_service.py`

## Tests y comandos

- `uv run --project services/api pytest services/api/tests/integration/test_temporal_refinement_search.py` — PASS (2 tests).
- `uv run --project services/api pytest services/api/tests/unit/test_search_service.py services/api/tests/integration/test_search.py services/api/tests/integration/test_parity_cli_api.py` — PASS (18 tests; una advertencia deprecación de Starlette/httpx).
- Ruff y mypy de servicio, router y tests — PASS.

## Decisiones y límites

- El orquestador aplica límites de candidatos/assets y timeouts, mantiene el
  orden/score y solo cambia `match_timestamp_ms` cuando el asset supera
  estrictamente la similitud base.
- La persistencia de `search_refinements`/evidence queda para T023; esta tarea
  conserva únicamente la analítica existente de `searches`.
- Un error de composición o una política inválida degrada a fallback base y no
  bloquea la búsqueda.

## Riesgos y trabajo pendiente

- T015/T018 deben ampliar los fallbacks con 403/404, corrupción, cancelación y
  rate-limit observables.
- T023 debe registrar el summary/evidence sin bytes de consulta ni assets.
- La preview/E2E WebdriverIO y el benchmark siguen pendientes.

## Revisión solicitada

Comprobar el orden inmutable del ranking, provenance/summary REST, generación
temprana de `search_id`, fallback fail-closed y ausencia de escrituras en
`frames`/VectorStore.
