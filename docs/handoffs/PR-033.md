# Handoff — PR-033

- **Proveedor y modelo usados**: orquestador `deepseek-v4-pro` (cierre) + implementadores
  `deepseek-official · deepseek-v4-flash` (PR-019…PR-034) + revisores
  `deepseek-official · deepseek-v4-flash` (agentes distintos).
- **Resumen**: Cierre de la fase 2 (Source SDK + primer crawler). Validación operativa real
  del quickstart con el mock (sin red) sobre Supabase local, y documentación del estado de la
  fase. La validación con **xvideos real** queda **bloqueada por diseño** (SEC-002) hasta la
  revisión legal/ToS/robots del humano.
- **Requisitos implementados**: cobertura completa de spec 002 (FR-001…FR-015, SEC-001…004,
  DATA-001…003, NFR-001…004) vía PR-019…PR-034. SC-001…SC-008 verificadas: SC-002/003/004/005
  con el mock end-to-end real; SC-002 sobre xvideos real pendiente de la puerta legal.
- **Archivos modificados**: `docs/STATUS.md`, `specs/002-source-sdk-crawler/quickstart.md`,
  `specs/002-source-sdk-crawler/spec.md` (estado), `docs/handoffs/PR-033.md`.
- **Decisiones**: la spec 002 queda en `IMPLEMENTING` (no `IMPLEMENTED`) hasta que el operador
  habilite xvideos (revisión legal + `sources.enabled=true`) y valide un backfill acotado
  real. La habilitación es un UPDATE de una fila en `sources` — no requiere código nuevo.
- **Tests añadidos**: validación manual del quickstart con cableado real (evidencias abajo).
  Gates: 280 pytest · mypy · ruff · pgTAP 139+ PASS (ver `## Comandos ejecutados`).
- **Comandos ejecutados** (Supabase local, 2026-08-16):
  - `xtrace-crawler sources --json` → mock enabled=true; xvideos enabled=false.
  - `xtrace-crawler backfill --source mock --limit 20` → job DISCOVER encolado.
  - `xtrace-crawler run-worker --once` → `{"processed": 103}`; **103/103 jobs done**;
    **50/50 vídeos `indexed`**; 390 frames con embeddings consultables.
  - `xtrace-crawler backfill --source mock --limit 20 --incremental` + worker →
    3 jobs, **0 duplicados** (siguen 50 vídeos / 390 frames) — SC-003.
  - `xtrace-crawler check-availability --source mock --limit 10` + worker → 10 done.
  - `xtrace-crawler backfill --source xvideos --limit 10` → **rechazado por el gate
    SEC-002** con error claro y exit 1 (robots/terms/review_date/enabled) — comportamiento
    correcto.
  - `uv run pytest` → 280 passed · `mypy`/`ruff` verdes · `supabase test db` → PASS.
- **Resultados**: quickstart 100% validado con el mock (SC-002/003/004/005 en cableado real);
  gate legal de xvideos verificado. Hallazgo de integración corregido en PR-034.
- **Limitaciones**: SC-002 contra xvideos **real** y la revisión legal/ToS/robots son
  responsabilidad del humano (SEC-002). Sin seed automático de `sources` (registro de fuentes
  es un UPDATE SQL del operador, documentado en `quickstart.md`).
- **Riesgos**: ninguno nuevo. Riesgo legal de xvideos abierto (puerta del humano).
- **Trabajo pendiente**: (1) humano: revisar ToS/robots de xvideos y rellenar el manifest;
  (2) humano: `update public.sources set manifest=..., enabled=true where name='xvideos';`;
  (3) operador: `backfill --source xvideos --limit N` acotado + verificar SC-002/003/005/006;
  (4) marcar spec 002 `IMPLEMENTED` y mergear la rama `feature/002-source-sdk-crawler` a
  `main` vía PR con CI verde y aprobación humana.
- **Instrucciones para el revisor**: revisar este handoff contra las evidencias de comandos;
  comprobar que spec 002 en `IMPLEMENTING` documenta la puerta legal; verificar `quickstart.md`
  ejecutable tal cual está escrito.

## Evidencias de la validación operativa (cableado real, mock, sin red)

| Paso | Resultado |
| --- | --- |
| `sources --json` | `mock` (enabled=true) · `xvideos` (enabled=false) |
| `backfill mock --limit 20` + `run-worker --once` | 103/103 jobs `done` · **50/50 `indexed`** |
| `backfill mock --incremental` + worker | 0 duplicados (50 vídeos · 390 frames) — SC-003 |
| `check-availability mock` + worker | 10/10 jobs `done` |
| `backfill xvideos` | rechazado por gate SEC-002 (exit 1, error tipado) |
| Gates | pytest 280 · mypy · ruff · pgTAP PASS |

## Puerta legal pendiente (SEC-002 — solo el humano)

```sql
-- 1. Revisar ToS/robots de xvideos (responsabilidad del humano) y documentarlo.
-- 2. Actualizar el manifest con la revisión y habilitar:
update public.sources
   set manifest = jsonb_set(jsonb_set(manifest, '{robots_reviewed}', 'true'),
                            '{terms_reviewed}', 'true')
where name = 'xvideos';
update public.sources
   set manifest = jsonb_set(manifest, '{review_date}', '"2026-MM-DD"')
where name = 'xvideos';
update public.sources set enabled = true where name = 'xvideos';
-- 3. Backfill acotado manual y verificación de SC-002/003/005/006.
```
