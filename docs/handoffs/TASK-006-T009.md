# Handoff TASK-006-T009

## Resumen

Se fijaron tests first para materialización in-memory: tipos permitidos,
deduplicación, corrupción, límites de bytes/píxeles, presupuesto de cantidad,
cleanup en fallo y éxito, rechazo explícito de `preview`/vídeo/desconocidos y
captura fail-closed de `Image.DecompressionBombError`.

## Tests y comandos

- `uv run --project services/api pytest services/api/tests/unit/test_refinement_assets.py`
  — PASS (6 tests).
- Ruff del test — PASS.

## Revisión solicitada

La suite no permite descargar vídeo; los bytes se mantienen solo durante la
llamada y el ownership de las imágenes aceptadas queda liberable mediante
`MaterializationResult.close()`.
