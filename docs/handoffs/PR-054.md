# Handoff — PR-054

- **Proveedor y modelo usados** (obligatorio para auditar coste): `provider: deepseek-official · model: deepseek-v4-flash`
- **Resumen**: Bootstrap del tercer servicio Python del monorepo,
  `services/api/` (paquete `xtrace_api`, **FastAPI** + uvicorn, ADR-0012):
  `pyproject.toml` con dependencia **editable** `xtrace-spike`
  (`[tool.uv.sources] path = "../search-spike"`, ADR-0011/0012), toolchain
  uv/ruff/mypy/pytest, `config.py` base (pydantic-settings, env
  `XTRACE_API_*` + convenios compartidos `SUPABASE_DB_URL` y
  `XTRACE_EMBEDDING_PROVIDER`), `main.py` mínimo con **`GET /health`**
  (FR-006, contracts §2 — no depende de la BD), CORS allowlist por env y
  lifespan que asegura `work_root`. Workflow GitHub Actions aditivo
  `python-api-quality` (paridad con `python-crawler-quality`, incl. hardening
  PR-037: actions por SHA, `permissions: contents: read`, sin pgTAP en CI).
  Dockerfile y README con paridad de estilo con el crawler. Sin lógica de
  búsqueda (llega en PR-055).
- **Requisitos implementados**: FR-006 (`GET /health`), SEC-001 (bind
  `127.0.0.1` por defecto en config/Dockerfile/README), SEC-004 (DSN de
  servidor `SUPABASE_DB_URL`, sin secretos en el repo), SEC-006 (config por
  env), NFR-001 (toolchain CPU local), NFR-003 (dep editable `xtrace_spike`,
  ADR-0011/0012), contracts §2 (contrato de `/health` estable), plan
  §Project Structure/§CI strategy. Trazabilidad PR-054 en docstrings de
  módulos, tests, workflow y README (constitución §3).
- **Archivos modificados** (allowed_paths del contrato, sin desviaciones):
  - `services/api/pyproject.toml` — **NUEVO**: `xtrace-api` 0.1.0,
    `fastapi`/`uvicorn`/`python-multipart`/`pydantic-settings` + dep editable
    `xtrace-spike`; extra opcional `siglip` (paridad spike/crawler; el
    quickstart lo documenta); dev: `httpx` (TestClient), `mypy`, `pytest`,
    `ruff`; `[tool.uv.sources]`; ruff (E/F/I/UP/B, line-length 100),
    mypy strict (tests con override), pytest (`testpaths=tests`).
  - `services/api/uv.lock` — **NUEVO**: lock reproducible (`uv sync --locked`
    en CI).
  - `services/api/xtrace_api/__init__.py` — **NUEVO**: `__version__` por
    metadata (patrón spike/crawler).
  - `services/api/xtrace_api/config.py` — **NUEVO**: `Settings` con
    `host`/`port` (default `127.0.0.1:8000`), `supabase_db_url` (alias
    `SUPABASE_DB_URL`), `embedding_provider` (alias `XTRACE_EMBEDDING_PROVIDER`),
    `work_root` (default `<tempdir>/xtrace-api`), `cors_origins` (default
    `["http://localhost:3000"]`); `get_settings()` cacheado (patrón del repo).
  - `services/api/xtrace_api/main.py` — **NUEVO**: `create_app()` + `app` con
    lifespan (asegura `work_root`), CORSMiddleware con allowlist por env y
    `GET /health` → `{"status":"ok","service":"xtrace-api","version":...}`
    (contracts §2, FR-006). Routers/exception handlers: PR-055/056.
  - `services/api/tests/__init__.py`, `tests/unit/__init__.py`,
    `tests/unit/test_smoke.py` — **NUEVOS**: smoke pytest (ver *Tests
    añadidos*).
  - `services/api/Dockerfile` — **NUEVO**: imagen uvicorn, build context raíz
    del repo (dep editable), bind `127.0.0.1` (SEC-001), paridad crawler.
  - `services/api/README.md` — **NUEVO**: toolchain, arranque local con
    uvicorn, tabla de env, Docker, seguridad.
  - `services/api/.python-version` (`3.11`), `services/api/.gitignore` —
    **NUEVOS** (paridad crawler + `work_root/`).
  - `.github/workflows/python-api-quality.yml` — **NUEVO**: aditivo;
    triggers `services/api/**` + `services/search-spike/**` + el propio
    workflow; pasos `uv sync --locked` → `ruff check` → `ruff format --check`
    → `mypy xtrace_api` → `pytest`; actions por SHA (v4/v5/v5, mismas que
    `python-quality.yml`); `permissions: contents: read`; sin pgTAP en CI
    (decisión paridad crawler PR-037, gates locales documentados).
  - `docs/handoffs/PR-054.md` — este handoff.
- **Decisiones tomadas**: mantener el prefijo `XTRACE_API_` para las vars del
  servicio y `validation_alias` (AliasChoices) para los convenios compartidos
  `SUPABASE_DB_URL`/`XTRACE_EMBEDDING_PROVIDER` (no duplicar convenios del
  spike/crawler; prefijadas aceptadas como alternativas); `work_root` default
  `<tempdir>/xtrace-api` (plan: "default tempdir"); `cors_origins` como lista
  JSON por env (parseo nativo de pydantic-settings); app vía factory
  `create_app()` (testeable, extensible en PR-055/056); extra `siglip`
  incluido ya en PR-054 porque el quickstart lo documenta y PR-055 no toca
  `pyproject.toml`.
- **Tests añadidos** (test-first: se escribieron antes que el paquete y
  fallaron por `Unable to determine which files to ship` / módulo inexistente;
  todos marcan requisito): `test_package_importable` (base),
  `test_spike_editable_dependency_resolvable` (ADR-0011/0012, NFR-003),
  `test_health_returns_contract` (FR-006 · contracts §2 — 200, JSON exacto,
  sin depender de la BD), `test_config_defaults` (SEC-001/006: bind local,
  port, work_root, CORS), `test_config_env_overrides` (SEC-006: env
  `XTRACE_API_*` + convenios), `test_get_settings_is_cached` (patrón repo).
- **Comandos ejecutados** (con salida REAL, worktree PR-054):
  - `uv sync` (UV_CACHE_DIR=/tmp/uv-cache-xtrace) → OK, genera `uv.lock`
    (xtrace-api 0.1.0 + xtrace-spike 0.1.0 editable).
  - `uv run ruff check .` → `All checks passed!` (7 archivos).
  - `uv run ruff format --check .` → `7 files already formatted` (1
    reformateado previamente con `ruff format`).
  - `uv run mypy xtrace_api` → `Success: no issues found in 3 source files`.
  - `uv run pytest` → `6 passed, 1 warning in 0.50s` (warning upstream:
    `StarletteDeprecationWarning: Using httpx with starlette.testclient is
    deprecated` — sin acción en esta fase).
  - Validación real (criterio Done): `uv run uvicorn xtrace_api.main:app
    --host 127.0.0.1 --port 8000` → `curl http://127.0.0.1:8000/health`
    → `HTTP 200` `{"status":"ok","service":"xtrace-api","version":"0.1.0"}`
    (contracts §2 exacto); `/nope` → 404; cabecera
    `access-control-allow-origin: http://localhost:3000` presente.
- **Limitaciones**: los tests de integración con BD (PR-055/056) harán skip
  limpio sin Supabase local; el warning de deprecación httpx/httpx2 de
  starlette es upstream y no bloquea; `pnpm verify` del skeleton no se ejecutó
  porque este PR no toca ningún archivo JS (solo paths `services/api/**` y el
  workflow nuevo — aditivo); el workflow se validará en el PR (no hay runner
  local).
- **Riesgos**: ninguno nuevo conocido; la API no se despliega (D4) y escucha
  solo en `127.0.0.1` (SEC-001) — el Dockerfile/README lo refuerzan.
- **Trabajo pendiente**: PR-055 (`POST /search`: media.py, search_service.py,
  schemas.py, deps.py, analytics.py, routers/search.py), PR-056 (`/stats`,
  `/videos/{id}`, TTL en lifespan), PR-057 (frontend), PR-058 (E2E + cierre).
- **Instrucciones para el revisor**: verificar (1) paridad del workflow con
  `python-crawler-quality.yml` (pins SHA, permisos, decisiones pgTAP/mypy);
  (2) contrato `/health` contra contracts §2; (3) que `config.py` respeta los
  convenios `SUPABASE_DB_URL`/`XTRACE_EMBEDDING_PROVIDER`; (4) que no se tocó
  nada fuera de `allowed_paths`; (5) gates locales reproducibles con los
  comandos del contrato.
