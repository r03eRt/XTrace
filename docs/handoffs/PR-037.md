# Handoff — PR-037

- **Proveedor y modelo usados** (obligatorio para auditar coste — p. ej. `provider: deepseek-official · model: deepseek-v4-flash`): `provider: deepseek-official · model: deepseek-v4-flash`
- **Resumen**: Hardening del workflow CI del crawler (`python-crawler-quality.yml`): actions pineadas por SHA completo, permissions mínimas del job y DECISION de pgTAP/integración documentada (converge F3 + security-review P2-CI).
- **Requisitos implementados**: NFR-001/NFR-003 (CI local-first, gates §CI strategy); security-review P2-CI (pin por SHA + permissions). Trazabilidad: spec 002 · plan §CI strategy.
- **Archivos modificados**: `.github/workflows/python-crawler-quality.yml` (único archivo de implementación).
- **Decisiones tomadas**:
  1. Pin por SHA completo de las 3 actions, mismas versiones que `python-quality.yml` del spike (fase 1): `actions/checkout@11d5960a326750d5838078e36cf38b85af677262` (v4), `astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86` (v5, dereferenciada de tag anotada), `actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065` (v5); cada una con comentario de versión.
  2. `permissions: contents: read` a nivel de job (mínimo; sin tokens de escritura ni id-token).
  3. DECISION documentada en el header del workflow: pgTAP (`supabase test db`) e integración contra Postgres/pgvector NO corren en CI (requieren el stack Supabase completo); gates locales `supabase start` + `supabase test db` + `uv run pytest`, misma política que `python-quality.yml` de la fase 1. Los tests de integración (`tests/integration/*`) ya hacen skip limpio en CI (`pytest.mark.skipif` sobre `_db_available()`).
  4. NO se tocaron triggers ni paths (verificado por diff: solo +24/−3 líneas, sin cambios en `on:`/`paths`).
- **Tests añadidos**: N/A (tarea de configuración/CI; validación automatizada equivalente: parseo YAML + checks estructurales).
- **Comandos ejecutados**:
  - `curl api.github.com` para resolver SHAs de tags v4/v5 y verificar commits reales (incluida dereferenciación de la tag anotada de setup-uv v5).
  - `UV_CACHE_DIR=/tmp/uv-cache-xtrace uv run --with pyyaml --no-project python -c "..."` → YAML válido; name, triggers, paths, permissions `{'contents': 'read'}`, uses pineadas por SHA y working-directory verificados.
- **Resultados**: YAML válido; estructura coherente con `python-quality.yml` (mismas versions de actions y mismo patrón de steps); commit `aa43386` (`ci(crawler): PR-037 pin actions por SHA + permissions + decision pgTAP local (security-review P2)`).
- **Limitaciones**: No se ejecutó `pnpm verify` (no aplica: solo YAML de CI, sin código Python/JS tocado). Los SHAs quedan fijados al punto actual de las tags v4/v5; actualizaciones futuras de versiones requieren commit explícito.
- **Riesgos**: Bajo. Las actions pineadas no reciben updates automáticos (intencional, supply-chain); renovación manual vía PR si una version queda obsoleta.
- **Trabajo pendiente**: Revisión independiente (`task-review`); orquestador marca estado.
- **Instrucciones para el revisor**: Verificar (1) que las 3 `uses` son SHA de 40 hex con comentario de versión, (2) `permissions: contents: read` en el job, (3) que `on:`/`paths` no cambiaron respecto a 19ecda4, (4) coherencia con `python-quality.yml` (mismas versiones v4/v5/v5), (5) exactitud del diff (+24/−3).
