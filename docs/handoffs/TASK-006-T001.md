# Handoff TASK-006-T001

## Resumen

La API ya puede reutilizar el contrato de adapters y el materializador seguro del
crawler sin duplicar parsers ni allowlists.

## Requisitos implementados

- FR-003: la dependencia `xtrace-crawler` queda disponible para usar solo adapters
  habilitados.
- SEC-001: el build conserva la frontera de `SafeHTTPClient`/allowlist del crawler.

## Archivos modificados

- `services/api/pyproject.toml`
- `services/api/uv.lock`
- `services/api/Dockerfile`

## Decisiones

- Se añadió `xtrace-crawler` como editable junto a `xtrace-spike`.
- El Dockerfile copia `services/crawler/pyproject.toml`, README y paquete fuente.
- No se modificaron `services/search-spike` ni el crawler.

## Tests y comandos

- `uv lock --check --project services/api` — PASS.
- `uv sync --frozen --no-dev --project services/api` — PASS; instaló API/crawler sin
  extra `siglip`.
- Import smoke de `xtrace_crawler` y `XvideosAdapter` — PASS.
- `docker build -f services/api/Dockerfile -t xtrace-api:t001 .` — PASS.

## Limitaciones y riesgos

- El build Docker descarga dependencias de runtime; no se ejecutó el contenedor porque
  la tarea solo valida empaquetado.
- La imagen local `xtrace-api:t001` es un artefacto de Docker, no un archivo del repo.

## Revisión solicitada

Comprobar que la dependencia editable, el lock y el contexto Docker no introducen
torch/secretos ni rompen el build anterior.
