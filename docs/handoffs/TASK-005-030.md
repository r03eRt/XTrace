# Handoff — TASK-005-030

## Resultado

La tarea queda **BLOCKED por falta de datos de benchmark**, no por un fallo de la
implementación del comparador. No se ha rebajado ningún mínimo ni se ha fabricado una
verdad temporal.

## Evidencia

- No existen sidecar, consultas ni `dense.json`/`adaptive.json` válidos en el repositorio.
- El corpus local actual tiene 43 vídeos: 42 en `<5m`, ninguno en `5–15m` y uno en `>15m`.
- La evidencia web histórica conserva URLs, embeddings y metadatos, pero no los bytes de
  las consultas ni una verdad temporal independiente.
- Las posiciones de thumbnails XVIDEOS son una convención de la galería pública; usarlas
  como verdad de una consulta derivada del mismo asset sesgaría el benchmark.
- La CLI `sampling-benchmark` compara observaciones ya producidas; no genera búsquedas ni
  observaciones por caso.

## Progreso operativo (no equivale al benchmark de adopción)

El 2026-08-18 se ejecutó una prueba acotada sobre la sección pública gratuita
`/tags/chile`, sin solicitar ni seguir la ruta Premium `/tags/p:1/chile`:

- `DISCOVER` incremental: máximo 30 vídeos, `limit=100` para no truncar la página de tags.
- 30 vídeos descubiertos e indexados por el flujo legacy; 0 errores.
- `REINDEX` adaptativo oficial, `run_id=bf2225ad-9a3a-4e89-a3e0-876569f14f6c`,
  `mode=adaptive`, `target_interval_seconds=120`, `max_frames=8`.
- Resultado durable: 30 completados, 0 fallidos, 0 omitidos, 122 frames efectivos;
  cada vídeo quedó entre 1 y 8 frames.
- Tramos observados: 12 vídeos `<5m`, 13 vídeos `5–15m` y 5 vídeos `>15m`.
- Los assets solicitados fueron únicamente thumbnails públicos `xv_N_t.jpg` de los
  hosts allowlisted; no se descargaron vídeos completos ni se conservaron bytes web.

Esta ejecución demuestra que el flujo de selección y reemplazo adaptativo funciona sobre
una fuente real, pero no autoriza SC-004..SC-008: aún faltan consultas positivas pareadas,
observaciones dense/adaptive y timestamps anotados de forma independiente.

## Cambios

- Ningún cambio de código ni de defaults.
- Contrato `specs/005-adaptive-frame-sampling/contracts/TASK-005-030.md` actualizado a
  `BLOCKED`.

## Próximos pasos para desbloquear

1. Preparar al menos 30 consultas positivas con `source`, `duration_ms` y timestamp de
   verdad verificado, cubriendo local + web y los tres tramos (≥3 por segmento no vacío).
2. Obtener las consultas web desde assets públicos permitidos y conservarlas solo en un
   directorio temporal fuera de Git.
3. Ejecutar dense (30 frames) y adaptive sobre las mismas consultas en índices aislados,
   exportar ambas listas de observaciones y ejecutar la CLI con mínimos 30/3.
4. Registrar el informe fail-closed y decidir humanamente si se adopta algún cambio.

## Limitaciones

Hasta completar esos pasos, `target_interval_seconds=120` continúa siendo el contrato de
la feature y el reindex de 15 s del corpus local solo es un experimento operativo.
