# Handoff TASK-006-T006

## Resumen

Se fijó el contrato antes de implementar los modelos nuevos: resumen de
refinamiento, procedencia del timestamp, estados cerrados y compatibilidad con
payloads legacy.

## Requisitos implementados

- DATA-001: tests de fuente, asset, posición, timestamp y contadores agregados.
- UX-001..003: tests de procedencia visible/typed, estados inválidos y fallback
  compatible sin campos nuevos.

## Archivos modificados

- `services/api/tests/unit/test_schemas.py`
- `tests/unit/api-contract.test.ts`

## Tests y comandos

- pytest dirigido — FAIL esperado en collection porque `RefinementSummary` y
  `TimestampProvenance` aún no existen; se implementan en T007.
- `pnpm vitest run tests/unit/api-contract.test.ts` — 1 PASS legacy, 2 FAIL
  esperados porque el esquema Zod aún ignora los campos nuevos.

## Limitaciones y riesgos

- Los campos nuevos son opcionales para preservar clientes legacy; T007 debe
  conservar esa compatibilidad al hacerlos estrictos cuando estén presentes.

## Revisión solicitada

Comprobar que el contrato cubre el payload aprobado, rechaza estados/asset kinds
desconocidos y no obliga a clientes antiguos a conocer el segundo pase.
