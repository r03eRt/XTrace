# Handoff — ADR-0013

```yaml
task_id: ADR-0013
spec_id: 001-visual-search-spike / 002-source-sdk-crawler
title: Registrar 8 frames por vídeo como índice global multi-proveedor
status: READY_FOR_REVIEW
assigned_agent: Codex
provider: openai
branch: main
worktree: /Users/robertomorgadoluengo/work/code/XTrace
requirements:
  - Documentar la política global de muestreo para un catálogo de miles o millones de vídeos.
  - Separar la validación histórica del spike de la estrategia futura de producción.
acceptance_criteria:
  - Existe un ADR aceptado que fija 8 frames/vídeo como índice base global.
  - El ADR documenta refinamiento bajo demanda, assets escasos y no reindexado automático.
  - Arquitectura, idea de producto y estado reflejan la decisión sin cambios de código.
dependencies:
  - ADR-0004
  - ADR-0007
allowed_paths:
  - docs/adr/0013-global-coarse-index-8-frames.md
  - docs/architecture/visual-search-spike.md
  - docs/PRODUCT_IDEA.md
  - docs/STATUS.md
  - docs/handoffs/ADR-0013.md
forbidden_paths:
  - services/**
  - supabase/**
  - specs/**
required_tests:
  - Validación de referencias y coherencia documental con rg.
  - Comprobación de formato Markdown y diff acotado.
reviewer: revisión documental independiente pendiente
started_at: 2026-08-17
completed_at: 2026-08-17
```

- **Proveedor y modelo usados**: OpenAI Codex (modelo de la sesión).
- **Resumen**: Registrada la decisión de usar 8 frames por vídeo como índice base global
  para un catálogo multi-proveedor, con refinamiento temporal bajo demanda.
- **Requisitos implementados**: decisión persistente y separación explícita entre el
  benchmark del spike (30 frames) y el catálogo global futuro (8 frames).
- **Archivos modificados**:
  - `docs/adr/0013-global-coarse-index-8-frames.md`
  - `docs/architecture/visual-search-spike.md`
  - `docs/PRODUCT_IDEA.md`
  - `docs/STATUS.md`
- **Decisiones tomadas**: no cambiar defaults del código ni reindexar el corpus actual;
  ambos requieren una tarea de implementación y benchmark posterior.
- **Tests añadidos**: ninguno; es una modificación documental.
- **Comandos ejecutados**: `rg` para referencias de frames/escala, `git diff --check` y
  `pnpm verify`.
- **Resultados**: `git diff --check`, Prettier, ESLint, TypeScript, Vitest y tests de
  Supabase pasan. `pnpm verify` falla únicamente en el smoke E2E porque el API local ya
  ocupa `127.0.0.1:8000`; el stub no arranca y la prueba recibe 10 resultados reales en
  lugar de los 3 stubbeados. También aparece el problema preexistente de la carpeta de
  screenshots ausente. No hay cambios de código asociados a esta tarea.
- **Limitaciones**: todavía no se ha medido recall ni error temporal con 8 frames sobre
  corpus multi-fuente; el ADR exige ese benchmark antes de reindexar.
- **Riesgos**: algunas fuentes pueden proporcionar menos de 8 assets visuales; el índice
  debe conservar el conteo real y no fabricar timestamps.
- **Trabajo pendiente**: crear tarea de implementación para configurar el muestreo global,
  diseñar el refinamiento y ejecutar el benchmark 8 vs 30/60.
- **Instrucciones para el revisor**: comprobar que el ADR no cambia silenciosamente el
  comportamiento del spike ni introduce cambios de código fuera del alcance.
