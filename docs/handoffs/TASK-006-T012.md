# Handoff TASK-006-T012

## Resumen

Se conectó la lógica de elegibilidad con `AdapterRegistry.get_enabled` y se
añadió el mapeo puro de metadatos indexados a candidatos. No se introdujo un
parser HTML ni una segunda allowlist en la API.

## Archivos modificados

- `services/api/xtrace_api/refinement/adapters.py`
- `services/api/xtrace_api/refinement/catalog.py`

## Tests y comandos

- `uv run --project services/api pytest services/api/tests/unit/test_refinement_adapters.py services/api/tests/unit/test_refinement_catalog.py` — PASS (8 tests).
- Ruff y mypy de ambos módulos — PASS.

## Limitaciones y riesgos

- La consulta SQL de `videos/sources` y la creación del registry por aplicación
  se integrarán en la orquestación de T014; este bridge no hace red ni DB.

## Revisión solicitada

Comprobar gate fail-closed, candidatos locales sin acceso de red y ausencia de
duplicación del parser/allowlist del crawler.
