# Handoff TASK-006-T017

## Resumen

Se añadieron pruebas de contrato y render para distinguir timestamps refinados,
timestamps del índice base y la degradación de disponibilidad en `/buscar`, sin
alterar el flujo de subida, carga, enlaces ni compatibilidad legacy.

## Requisitos implementados

- UX-001: badge de procedencia `refined_asset` y `base_index`.
- UX-002/UX-003: resultados base visibles y aviso comprensible para `limited` y
  `unavailable`.
- SC-008: payload sin `refinement` ni provenance sigue siendo válido y se renderiza
  sin badges ni aviso.

## Archivos modificados

- `tests/unit/buscar-page.test.tsx`
- `tests/unit/api-contract.test.ts`

## Tests y resultados

- `pnpm exec vitest run tests/unit/buscar-page.test.tsx tests/unit/api-contract.test.ts` — **30 passed**.
- `pnpm exec prettier --check ...` — **PASS**.
- `pnpm exec eslint ...` — **PASS**.
- `pnpm typecheck` — **PASS**.

## Decisiones y cumplimiento

Los tests no afirman exactitud temporal ni requieren vídeo, CAPTCHA, autenticación,
DRM o datos biométricos. No se cambió la API ni se persiste la imagen de consulta.

## Limitaciones y revisión

La implementación de producción correspondiente queda en TASK-006-T019. Requiere
revisión independiente antes de cerrar la trazabilidad de esta tarea. La revisión
de `/root/review_task_005_005` fue **APPROVED** el 2026-08-19.
