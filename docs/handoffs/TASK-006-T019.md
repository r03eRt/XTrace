# Handoff TASK-006-T019

## Resumen

La página `/buscar` ahora conserva el primer pase y muestra de forma explícita
la procedencia del timestamp. Los timestamps sustituidos por evidencia pública
se etiquetan como refinados y aproximados; los del índice base se distinguen
sin presentarlos como exactos. Los estados de disponibilidad limitada no
ocultan los resultados encontrados.

## Archivos modificados

- `src/features/search/buscar-page.tsx`
- `tests/unit/buscar-page.test.tsx`
- `tests/unit/api-contract.test.ts` (ajuste de estrechamiento TypeScript)

No fue necesario cambiar `src/lib/api/xtrace.ts`: el cliente ya valida de forma
nullish el resumen y la provenance.

## Requisitos y decisiones

- `refined_asset` + `improved`: badge «Timestamp refinado (aproximado)».
- `base_index`: badge «Timestamp del índice base (aproximado)».
- `limited`, `unavailable` y `failed`: aviso «Disponibilidad limitada» con
  fallback al resultado base.
- Respuesta legacy: sin aviso ni badge, manteniendo el render previo.

## Validación

- `pnpm exec vitest run tests/unit/buscar-page.test.tsx tests/unit/api-contract.test.ts` — **30 passed**.
- Prettier de archivos frontend/tests — **PASS**.
- ESLint de archivos frontend/tests — **PASS**.
- `pnpm typecheck` — **PASS**.

## Cumplimiento y riesgos

No se afirma exactitud temporal ni se cambió el flujo de upload/loading/enlaces.
La revisión independiente de `/root/review_task_005_005` fue **APPROVED** el
2026-08-19; también verificó la regresión del timeout efectivo.
