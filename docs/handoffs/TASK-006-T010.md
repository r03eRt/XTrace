# Handoff TASK-006-T010

## Resumen

Se fijaron tests first para el gate de adapters y el mapeo de metadatos del
índice a `RefinementCandidate`. Los casos cubren mock habilitado, fuente real
deshabilitada/desconocida (incluido `sources.enabled=false` con manifest válido),
preservación de `VideoSource` en `get_video`, vídeos locales y candidatos
incompletos sin red.

## Tests y comandos

- `uv run --project services/api pytest services/api/tests/unit/test_refinement_adapters.py services/api/tests/unit/test_refinement_catalog.py`
  — PASS (8 tests).
- Ruff, formato y mypy de producción/tests — PASS.

## Revisión solicitada

Revisión independiente `/root/review_task_005_001` — APPROVED (2026-08-19).
