# Handoff — PR-001

## Contrato de tarea (skill task-execution)

```yaml
task_id: PR-001
spec_id: 001-visual-search-spike
title: Bootstrap del servicio Python + CI
status: READY_FOR_REVIEW
assigned_agent: implementer
provider: deepseek
branch: feature/001-visual-search-spike-PR-001-bootstrap-python-service-ci
worktree: raíz del repo
requirements:
  - FR-017 (CLI interna Typer)
  - ADR-0003 (servicio Python en services/search-spike, aislado del skeleton JS)
  - ADR-0008 (CLI como interfaz de validación; FastAPI/frontend diferidos)
acceptance_criteria:
  - ruff + mypy + pytest pasan en verde
  - "xtrace-spike --help" funciona (exit 0, muestra help)
  - workflow python-quality ejecuta: ruff check + ruff format --check + mypy + pytest
  - pipeline JS existente (quality.yml) intacta
  - compose.yaml sigue válido (solo añadido servicio spike)
  - handoff en docs/handoffs/PR-001.md
dependencies: []
allowed_paths: [services/search-spike/**, .github/workflows/python-quality.yml,
  compose.yaml (solo añadir servicio spike), docs/handoffs/PR-001.md]
forbidden_paths: [tasks.md, docs/STATUS.md, specs/**, docs/adr/**, package.json,
  pnpm-lock.yaml, .github/workflows/quality.yml, e2e.yml, security.yml,
  spec-compliance.yml, src/**, tests/**, supabase/**]
required_tests: [pytest smoke: importa xtrace_spike y ejecuta "xtrace-spike --help" con exit 0]
reviewer: pendiente (agente distinto vía task-review)
started_at: 2026-08-14
completed_at: 2026-08-14
```

- **Resumen**: Bootstrap del servicio Python del spike de búsqueda visual:
  paquete `xtrace_spike` con CLI Typer mínima (`--help`/`--version`, sin
  subcomandos de negocio), toolchain `ruff`/`mypy`/`pytest`, entorno `uv`
  con lock comprometido, `Dockerfile`, servicio `spike` en `compose.yaml`
  (perfil propio, sin tocar `web`) y workflow GitHub Actions `python-quality`
  aditivo. Todo verde en local sobre Python 3.11.
- **Requisitos implementados**:
  - **FR-017** (CLI interna): `xtrace-spike --help` sale con 0 y muestra ayuda;
    `--version` muestra `xtrace-spike 0.1.0`. Los comandos `index/search/
    benchmark/exclude/stats` llegan en PRs posteriores (PR-011/014/016) según
    `specs/001-visual-search-spike/contracts/README.md`.
  - **ADR-0003**: servicio Python en `services/search-spike/` aislado del
    skeleton Next.js; job de CI dedicado sin alterar la pipeline JS.
  - **ADR-0008**: interfaz Typer; sin FastAPI ni frontend.
- **Archivos modificados** (14; ninguno fuera de `allowed_paths`):
  - `services/search-spike/pyproject.toml` — PEP 621; runtime `typer`; dev
    group `ruff`/`mypy`/`pytest`; entry point `xtrace-spike`; config de
    ruff (E/F/I/UP/B, line-length 100), mypy strict + override relajado para
    tests, pytest testpaths.
  - `services/search-spike/uv.lock` — lock reproducible generado por uv 0.12.1.
  - `services/search-spike/.python-version` — `3.11`.
  - `services/search-spike/.gitignore` — artefactos Python locales.
  - `services/search-spike/README.md` — setup, comandos de calidad, Docker, CI.
  - `services/search-spike/Dockerfile` — `python:3.11-slim`, instala el
    paquete (solo runtime), `ENTRYPOINT xtrace-spike` + `CMD --help`.
  - `services/search-spike/xtrace_spike/__init__.py` — `__version__` vía
    `importlib.metadata` con fallback estático.
  - `services/search-spike/xtrace_spike/cli.py` — app Typer vacía con
    `--help`/`--version` y `no_args_is_help=True`.
  - `services/search-spike/tests/__init__.py` y `tests/unit/__init__.py`.
  - `services/search-spike/tests/unit/test_cli_smoke.py` — 3 tests smoke.
  - `.github/workflows/python-quality.yml` — CI Python (nuevo, aditivo).
  - `compose.yaml` — añadido servicio `spike` (build `services/search-spike`,
    `profiles: ["spike"]`, `command: --help`); `web` NO modificado.
  - `docs/handoffs/PR-001.md` — este archivo.
- **Decisiones tomadas**:
  - `uv` como gestor de entorno y `uv.lock` comprometido (reproducibilidad en
    CI con `uv sync --locked`). Build backend `hatchling` (PEP 621).
  - Dev deps en `[dependency-groups]` (PEP 735): `uv sync` instala dev en
    local/CI; el Dockerfile hace `pip install .` (solo runtime, imagen mínima).
  - Python 3.11 fijado en 4 sitios: `requires-python = ">=3.11,<3.12"`,
    `.python-version`, workflow `python-version: "3.11"` y `python:3.11-slim`.
  - mypy `strict` para el paquete y override sin strict para `tests.*`.
  - Servicio compose bajo `profiles: ["spike"]`: `docker compose up` por
    defecto sigue levantando solo `web` (cero impacto en el flujo JS).
  - Workflow con filtros de `paths` (solo cambios en `services/search-spike/**`,
    el propio workflow o `compose.yaml`) y grupo de concurrencia; triggers
    `pull_request` + `push main` como `quality.yml`.
  - CLI sin subcomandos en este PR (alcance de bootstrap); contratos ya definidos
    en `specs/001-visual-search-spike/contracts/README.md` para los PRs siguientes.
- **Tests añadidos** (`tests/unit/test_cli_smoke.py`, 3 tests):
  1. `test_package_import_and_version` — importa `xtrace_spike` y expone versión.
  2. `test_cli_help_exits_zero_and_shows_help` — `--help` vía `CliRunner`:
     exit 0 y salida con `Usage:`.
  3. `test_cli_help_via_installed_entry_point` — ejecuta el console script
     `xtrace-spike --help` instalado (subprocess): exit 0.
- **Comandos ejecutados** (en `services/search-spike`, salvo indicación; exit codes reales):
  - `uv sync --python 3.11` → **0** (Python 3.11.0; typer 0.27.1, ruff 0.16.3,
    mypy 1.20.2, pytest 8.4.2; genera `uv.lock`).
  - `uv run ruff check` → **0** ("All checks passed!").
  - `uv run ruff format --check` → **0** ("6 files already formatted").
  - `uv run mypy xtrace_spike tests` → **0** ("Success: no issues found in 5 source files").
  - `uv run pytest` → **0** (3 passed).
  - `uv run xtrace-spike --help` → **0** (Usage + opciones).
  - `uv run xtrace-spike --version` → **0** ("xtrace-spike 0.1.0").
  - `docker compose config` → **0** (con `.env.local` transitorio vacío porque
    el repo no lo tiene y `web` lo referencia — condición preexistente; el
    archivo se creó y eliminó dentro del mismo comando).
  - Parseo YAML de `.github/workflows/python-quality.yml` → **0** (vía Ruby YAML).
  - `docker build`/`docker run` → no ejecutables: el daemon de Docker Desktop
    no está corriendo en esta sesión (limitación del entorno, no del Dockerfile).
- **Resultados**: los 4 gates (ruff check, ruff format --check, mypy, pytest) y
  `--help` en verde sobre Python 3.11. Compose válido. Workflow YAML válido.
  `quality.yml` y el resto de la pipeline JS intactos (no tocados).
- **Limitaciones**:
  - Docker daemon inactivo en la sesión → el `Dockerfile` no se pudo construir
    aquí; se validó estructura/compose. Se construirá en revisión o CI.
  - El workflow no se ha ejecutado aún en GitHub (requiere push/PR); la secuencia
    exacta de comandos se replicó localmente con exit 0.
  - CLI sin subcomandos de negocio por diseño (bootstrap).
- **Riesgos**: ninguno relevante. Riesgo menor: drift de versiones en CI
  (`setup-uv` instala la última uv estable; el lock es forward-compatible).
- **Trabajo pendiente**: PR-002..004 (abstracciones+fake), PR-011/014/016
  (subcomandos de la CLI), build Docker en CI o con daemon activo, y ejecución
  real del workflow tras push/PR.
- **Instrucciones para el revisor**:
  1. `git diff main...HEAD --stat` y verificar que solo toca los 14 archivos de
     `allowed_paths` (en especial: `quality.yml`, `e2e.yml`, `security.yml`,
     `spec-compliance.yml`, `package.json` y `pnpm-lock.yaml` sin cambios).
  2. En `services/search-spike`: `uv sync --locked`, `uv run ruff check`,
     `uv run ruff format --check`, `uv run mypy xtrace_spike tests`,
     `uv run pytest`, `uv run xtrace-spike --help`.
  3. `docker compose config` (o `docker compose --profile spike build spike` si
     hay daemon).
  4. Revisar que `compose.yaml` conserva `web` byte a byte respecto a `main`.
  5. Confirmar trazabilidad: commits referencian PR-001/FR-017/ADR-0003.
