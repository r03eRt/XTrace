# Handoff TASK-006-T021

## Resumen

Se escribieron primero los tests del contrato de analítica de refinamiento.
Cubren persistencia idempotente, contadores, estados, evidencia agregada sin
media, RLS deny-by-default, cascada y retención TTL.

## Archivos modificados

- `services/api/tests/unit/test_refinement_analytics.py`
- `services/api/tests/integration/test_temporal_refinement_schema.py`

## Cobertura

- `record_refinement` escribe todos los contadores y los cinco estados válidos.
- Las reintentos requieren `ON CONFLICT(search_id)` y una sola fila de resumen.
- La evidencia no acepta bytes ni columnas de consulta/media.
- Los contadores negativos fallan antes de llegar a persistencia.
- Server-side puede insertar; `anon` y `authenticated` no pueden leer ni insertar.
- El purge TTL elimina summary/evidence expirados por cascade y conserva los
  registros recientes.

## Validación

- Ruff format/check — **PASS**.
- Mypy — **PASS**.
- Pytest de la unidad e integración requerido tras T023 — **18 passed**.
  Antes de T023, la colección fallaba explícitamente porque `record_refinement`
  aún no existía; tras añadir el writer, la prueba RLS ejecuta cada denegación
  en una transacción separada para no reutilizar una transacción abortada.

## Trabajo pendiente

T023 debe implementar el writer server-side y conectar el summary de cada
`POST /search`; después estos tests deben pasar y requieren revisión
independiente. Revisión de `/root/review_task_005_005`: **APPROVED**
(2026-08-19).
