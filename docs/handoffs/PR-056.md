# Handoff — PR-056

- **Proveedor y modelo usados**: `provider: deepseek-official · model: deepseek-v4-flash`
- **Resumen**: Implementado `GET /stats` (FR-007, contracts §3) con los **mismos campos que la CLI `stats`** (videos/frames/vectors + backend + embedding_provider) resolviendo backend/proveedor con la **misma regla que la CLI** (`deps.get_search_components` → `build_backend` + `resolve_embedding_provider`; paridad por construcción); `GET /videos/{id}` (FR-008, contracts §4) con la ficha completa (metadatos, `source` vía join `sources`, `excluded`), **400** `invalid_uuid` (validación sin tocar BD, paridad SC-006), **404** `video_not_found` y **503** `index_unavailable`; y el **TTL de `searches` sin migración** (FR-012 · DATA-001 · data-model.md): env `XTRACE_API_SEARCHES_TTL_DAYS` (default 30) + `XTRACE_API_SEARCHES_TTL_CLEANUP_MIN` (default 60), **purge inicial al arrancar + loop periódico en el lifespan**, ambos best-effort y solo con backend postgres.
- **Requisitos implementados**: FR-007, FR-008, FR-011 (400 `invalid_uuid` / 404 `video_not_found`), FR-012 (TTL) · SEC-004 (PgRepo service-side, RLS intacta) · SEC-005 (sin media) · DATA-001 (TTL por `created_at`, sin migración) · UX-001 · contracts §3/§4/§5.
- **Archivos modificados**:
  - `services/api/xtrace_api/routers/stats.py` (nuevo): `GET /stats`.
  - `services/api/xtrace_api/routers/videos.py` (nuevo): `GET /videos/{id}`, `VideoCardError` (400/404/503) y lectura vía `PgRepo` con join a `sources`.
  - `services/api/xtrace_api/analytics.py`: `delete_expired_searches` (repo inyectable), `searches_ttl_round` (best-effort) y `searches_ttl_loop` (periódico).
  - `services/api/xtrace_api/config.py`: sección TTL (`searches_ttl_days`, `searches_ttl_cleanup_min`, con límites pydantic).
  - `services/api/xtrace_api/schemas.py`: modelos `Stats` y `VideoCard` del contrato.
  - `services/api/xtrace_api/main.py`: routers stats/videos, handler de `VideoCardError` y TTL en el lifespan (cancelación en shutdown).
  - Tests: `unit/test_analytics.py` (nuevo), `integration/test_stats.py` (nuevo), `integration/test_videos.py` (nuevo).
  - `docs/handoffs/PR-056.md` (este archivo).
- **Decisiones tomadas**:
  - Paridad con la CLI por construcción: `embedding_provider` emite el `model_id` real (el fake es `fake-deterministic-hash`; el "(fake o SigLIP)" del contracts §3 se interpreta como model_id, coherencia FR-007).
  - Ficha en modo in-memory → **503 sin intentar conectar al DSN por defecto** (mismo criterio documentado de `record_search` en PR-055: no tocar la BD sin `SUPABASE_DB_URL`; evita colgarse).
  - TTL con repo inyectable (`repo=` kwarg) para el test unit con repo fake; el SQL es exactamente el del data-model.md.
  - `published_at` normalizado a UTC (serialización estable, formato Z del contrato).
- **Tests añadidos** (17 nuevos; baseline 34+6 de PR-055 intactos): `unit/test_analytics.py` (10): SQL TTL con repo fake, rowcount, round best-effort, límites/defaults de config; `integration/test_stats.py` (4: 2 sin BD + 2 skipif BD): contrato §3 in-memory (vacío y sembrado), paridad con la cadena CLI `stats` sobre postgres, **TTL ejecutado en el lifespan** (vencida borrada, reciente conservada); `integration/test_videos.py` (7: 4 sin BD + 3 skipif BD): ficha 200 del ejemplo del contracts §4 (fake), nullables, 400 sin tocar BD, 404, 503 in-memory, ficha real con join `sources`, 404 real.
- **Comandos ejecutados** (en `services/api/`, `UV_CACHE_DIR=/tmp/uv-cache-xtrace`):
  - `uv sync --locked` (venv nuevo) → OK
  - `uv run ruff check .` → All checks passed
  - `uv run ruff format --check .` → 27 files already formatted
  - `uv run mypy xtrace_api` → Success: no issues found in 12 source files
  - `uv run pytest` → **51 passed, 11 skipped** (los 11 saltados son integración con BD; el stack local está caído en este entorno — puerto 55322 acepta TCP pero PostgreSQL no responde —, skip limpio patrón PR-055)
  - Smoke TestClient: `/stats` 200 contrato §3; `/videos/{uuid}` 503 in-memory; `/videos/bad` 400 `invalid_uuid`; `/health` 200 intacto.
- **Limitaciones**: los 5 tests de integración nuevos (y los 6 de PR-055) requieren `supabase start` para ejecutarse; verificar con `uv run pytest` con BD local. El TTL por `created_at` no es expiración real (limitación aceptada en data-model.md). `PgRepo.connect` no admite `connect_timeout`: con `SUPABASE_DB_URL` fijada y BD silenciosa el purge inicial podría tardar (misma exposición preexistente de PR-055 en `record_search`).
- **Riesgos**: la ficha y el TTL dependen de la BD (503 si cae, ya cubierto por el handler global de psycopg); el loop TTL se cancela en shutdown (suppress CancelledError).
- **Trabajo pendiente**: PR-058 (E2E + cierre); ejecutar integración con Supabase local; el revisor puede validar `/stats` y `/videos/{id}` contra el índice real con `uvicorn xtrace_api.main:app` (quickstart).
- **Instrucciones para el revisor**: verificar paridad FR-007 (misma cadena que CLI `stats`), el 400/404/503 del contracts §5 (cuerpos exactos), TTL sin migración (env del data-model.md, purge inicial + loop), y que `services/search-spike/**` quedó intacto. Ejecutar los tests de integración con Supabase local levantado.
