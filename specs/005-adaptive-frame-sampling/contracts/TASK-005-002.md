# TASK-005-002 — Selección adaptativa de assets web

```yaml
task_id: TASK-005-002
spec_id: 005-adaptive-frame-sampling
title: Selección adaptativa de assets web permitidos
status: DONE
assigned_agent: luna_task_005_002
provider: openai
model: gpt-5.6-luna
reasoning: max
branch: feature/005-adaptive-frame-sampling
worktree: /Users/robertomorgadoluengo/work/code/XTrace
requirements: [FR-005, FR-006, FR-007, FR-008, SEC-001, SEC-002, SEC-004, SC-001, SC-002]
acceptance_criteria:
  - El crawler selecciona 1..8 frames tras expandir solo assets permitidos.
  - Deduplica posición/timestamp y pHash exacto antes de seleccionar.
  - No fabrica timestamps y cierra imágenes descartadas.
  - No añade acceso a vídeos completos ni nuevos tipos de asset.
dependencies: [TASK-005-001]
allowed_paths:
  - services/crawler/xtrace_crawler/pipeline.py
  - services/crawler/tests/integration/test_pipeline.py
  - specs/005-adaptive-frame-sampling/contracts/TASK-005-002.md
  - docs/handoffs/TASK-005-002.md
forbidden_paths: [services/search-spike/**, supabase/**, src/**, specs/005-adaptive-frame-sampling/tasks.md]
required_tests:
  - uv run --offline --no-sync ruff check .
  - uv run --offline --no-sync mypy xtrace_crawler
  - uv run --offline --no-sync pytest
reviewer: review_task_005_002 (gpt-5.6-luna Max) — APPROVED
started_at: 2026-08-17
completed_at: 2026-08-17
implementation:
  - La expansión de assets conserva `position` en `IndexedFrame` cuando la fuente lo declara.
  - La normalización y deduplicación preceden a la selección compartida: primera posición,
    timestamp válido y pHash exacto estable.
  - Los timestamps negativos, fuera de duración, no enteros o ausentes se conservan como
    `None`; no se interpolan valores.
  - Los frames descartados cierran su imagen PIL; los seleccionados se cierran después del
    embedding existente.
  - `CrawlerPipeline` conserva `INDEX_VIDEO` legacy cuando `sampling_policy=None`; la
    selección adaptativa (120 s, máximo 8) solo se activa con una política explícita.
  - Los helpers de preview cierran los frames PIL ya creados si la extracción falla a
    mitad del lote; no se dejan imágenes parciales abiertas.
  - No se añadió ningún tipo de asset ni acceso a vídeo.
validation:
  - targeted_tests: PASS (13)
  - ruff: PASS
  - ruff_format_check: PASS
  - mypy: PASS
```

Aplicar `task-execution` y test-first. Reutilizar `xtrace_spike.sampling`; no cambiar su
contrato. No acceder a vídeos completos, corpus real ni red externa.
